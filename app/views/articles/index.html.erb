<h1>All Articles </h1>

<ul id="articles">
  <% @articles.each do |article| %>
    <li>
        <%= link_to article.title, article_path(article), class: 'article_title', id: "article_#{article.id}" %> 
    </li>
  <% end %>
<br>
  <% if logged_in? %>
    <h5> <i> <%= link_to "New", new_article_url%><i> <h5>

  <% end %>
<br>
</ul>


    